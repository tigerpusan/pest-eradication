name: pest_eradication
description: Pest Eradication stage + audio + art patch
publish_to: "none"
version: 0.1.5+6
environment:
  sdk: ">=3.3.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.8.0
  shared_preferences: ^2.3.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/web/game.html
    - assets/icon/app_icon.png
